name: Validate Plugin Library

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  validate:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate library.json
        shell: pwsh
        run: ./scripts/validate-library.ps1
